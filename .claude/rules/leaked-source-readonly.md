---
paths:
  - "src/**"
---

# The repo-root `src/` is read-only reference

This tree is a mirror of leaked Claude Code CLI source (see root `README.md`). It has **no build tooling** here and is **not** part of any workflow.

- **Do not modify these files.** If a task seems to require editing here, stop — it almost certainly belongs in `cursor-claude-extension-bridge/` instead.
- Consult it only as reference (CLI flags, `stream-json` event shapes) when working on the extension.
